/*
modify the class written in first assignment

add more methods.

write the method to compute the average marks of two students.	
	(static)/(non-static)


write the method to compare marks of two students

	name of the method will be isEquals

	return type of the method will be boolean

	if the marks of two students are same , method should
	return true otherwise false.
write the method to compute the avg marks on n students.

		showAvg(s1,s2);
		showAvg(s1,s2,s3);
		showAvg(s1,s2,s3,s4);
*/




